let object = {

    firstName: 'onkar',
    secondName: '',

};

// if we want to know whether a property is defined in the object or not we did was

console.log(object.age);

// now say if we want to knwo in teh form of true or false then we use 

console.log('age: ' in object);