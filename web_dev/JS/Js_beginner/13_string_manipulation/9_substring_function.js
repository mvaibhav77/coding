let str = "This is string in js";

// for substring we use function 
//substr( index start  , length of word  ) : 

let substr=str.substr(8,12);

console.log(substr);

// another funciton is also used to gather the substring 

// .substring(index strt , index end)

let substr2 = str.substring (8 , 20);

console.log(substr2);
