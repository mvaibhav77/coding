
let str = "onkar patel" ;

// now say if we want to print onkar in one line and patel in other 

// we can do this by not the above method but this method : using (\n) instead

let str2="onkar \n patel" ;

console.log(str2);

