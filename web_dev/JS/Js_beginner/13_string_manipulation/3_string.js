let str ="onkar \t patel";

// if we want ot add spaces which is more than one space between strings we use tab space function also denoted by : (\t)

console.log(str);

// also say if we want ki hamare styring mei \ aana chaiyea we do this way 

let str2 = "onkar \\ patel";

// so here the first \ is not counted as character but as space character whereas the second \ is counted as character

console.log(str2);

//if we want " in outr string we use  : "

let str3 = "onkar \" patel";

console.log(str3);

