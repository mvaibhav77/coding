let user;

// user="Onkar";

// say if we dont initialise anything to the declared variable it will be considered as undefined if we dont want this output we use the nullish collising operator (??) instead saying that if in case it is left undefined display say guest user instead of undefined 

alert(user ?? "Guest User");