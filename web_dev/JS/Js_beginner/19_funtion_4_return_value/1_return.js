// if we want to return a value means we want to store it somewhere in out code in  a variable

// we do this storing process by calling the funtion aas to assigning it to a variable

function add(a, b) {
  
    let v = a + b;

  // or no need to declare this and write retunr a+b or (a+b)

  return v;

}

let c = add(3, 5);

document.write(c);

// if we dont return it adn store in c and display it will display undefined instead

//?  if we want to return multiple values in output then store it as an array in v and return and display 

//? return kinda can say like break jo pehal return statement hoga whi program execution bnd krke out of function next line pe chala jaata hai .... toh return ke bad koi code ya dusara return statement is useless 

