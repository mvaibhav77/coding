let isLoggedIn = 0;
//let isLoggedIn = 1;

// ternary operators are used where we need to assign value to someone or a single line if else code hai we dont use it at places where there are multiple lines of if else code  

let option = isLoggedIn == 1 ? "LOGOUT" : "LOGIN";

//             condition    true output , false output

document.write(option);